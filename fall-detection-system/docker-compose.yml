version: "3.8"

services:
  mqtt:
    image: eclipse-mosquitto
    ports:
      - "1883:1883"

  fog:
    build:
      context: .
      dockerfile: docker/fog.Dockerfile
    depends_on:
      - mqtt
    environment:
      - MQTT_BROKER=mqtt
    ports:
      - "8000:8000"
